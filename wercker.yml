box: wercker/nodejs
services:
  - mies/couchdb
# Build definition
build:
  steps: 
    - npm-install
    - npm-test
    -script:
      - name: curl
      - code: |-
          curl $$WERCKER_COUCHDB_HOST$$:5984

